from building import *
import os

cwd     = GetCurrentDir()
CPPPATH = [cwd]
CPPPATH += [cwd + "/src"]
CPPPATH += [cwd + "/inc"]
src     = Glob('src/*.c')
# src     += Glob('.test/test_esp32.c')

libs = DefineGroup('libs', src, depend = [''], CPPPATH = CPPPATH)

Return('libs')
